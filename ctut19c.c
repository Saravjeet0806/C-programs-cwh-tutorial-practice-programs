#include <stdio.h>

int main()
{
    int x, y, z;
    printf("Enter no1: \t");
    scanf("%d", &x);
    printf("\nEnter no2: \t");
    scanf("%d", &y);
    z = x + y;
    printf("The sum is %d", z );
}
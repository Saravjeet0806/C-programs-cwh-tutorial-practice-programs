void star_pattern() //this will not do anything and will show error

{
    int a;
    printf("Enter how many stars you want:\n", a);
    scanf("%d", &a);
    for (int i = 0; i < a; i++)
    {
        printf('*');
    }
}
